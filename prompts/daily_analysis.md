# daily_analysis

目标：

- 读取当日日志与必要上下文
- 输出一篇可读的每日报告
- 产出后续可被状态构建器稳定提取的结构

最小输出要求：

- 标题
- 日志整理
- 多维度分析
- 综合建议
- 今日值得被记住的一句话

兼容要求：

- 允许缺少 `认知盲区诊断`
- 若存在 frontmatter，至少包含 `date`、`type`、`title`、`tags`
- 若不存在 frontmatter，不阻断正文输出

安全约束：

- 建议和摘要应可在看板安全展示
- 不把具体私人事件细节写入机器状态字段
